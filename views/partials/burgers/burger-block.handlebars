<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{devour}}">
		{{#if sleep}}Hold for Pickup!{{else}}Eat Me!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
